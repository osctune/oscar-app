import React from 'react';

const Title = (_props) => {
    return (
        <div className="title">
            <h1>{APP_TITLE}</h1>
            <h2>Open Source Concurrent Address Reducer</h2>
        </div>
    );
};

export default Title;